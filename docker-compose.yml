services:
  db:
    image: mysql:9.5
    volumes:
      - data:/var/lib/mysql
    environment:
      MYSQL_DATABASE: obs
      MYSQL_USER: user
      MYSQL_PASSWORD: pass
      MYSQL_ROOT_PASSWORD: rootpass
    restart: always
    ports:
      - "3307:3306"

  obs:
    build:
      context: .
    working_dir: /app
    volumes:
      - .:/app
      # Keep image-installed Agent engine dependencies when the source tree is bind-mounted.
      - fcm_vue_node_modules:/app/FCM/vueFCM/node_modules
      - fcm_agent_node_modules:/app/mcp-server/node_modules
    ports:
      - "8000:8000"
    links:
      - db
    environment:
      DJANGO_SUPERUSER_USERNAME: admin
      DJANGO_SUPERUSER_EMAIL: admin@example.com
      DJANGO_SUPERUSER_PASSWORD: password 

    env_file:
      - .env

  cns:
    build:
      context: ./CNS/vueCNS
    working_dir: /app
    volumes:
      # Mount the vue source code here
      - ./CNS/vueCNS/src:/app/src
      # Mount static images at /static/CNS to match production path structure
      - ./CNS/static/CNS:/static/CNS
    ports:
      # Expose the Vite dev server port
      - "3012:3012"

volumes:
  data:
  fcm_vue_node_modules:
  fcm_agent_node_modules:
